<l-counter l-class="container"
           l-symbol-class="all-background font-color"
           l-disabled-class="all-background"
           l-count-class="all-background input-size"
           count="{{count}}"
           min="{{min}}"
           max="{{max}}"
           bind:linout="onOverStart"
>

</l-counter>